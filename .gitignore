# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Next.js build output — never commit; stale routes-manifest.json in git
# was serving old routes on Railway (the /guides 404 bug)
.next/
frontend/.next/

# --- hardened by consolidation snapshot: nested deps, venvs, secrets, data ---
node_modules/
**/node_modules/
.venv/
**/.venv/
backend/.venv/
venv/
__pycache__/
*.pyc
.env
.env.*
!.env.example
!**/.env.example
*.db
*.sqlite
*.sqlite3
dist/
build/
.DS_Store

# TypeScript incremental build artifacts — never commit or upload
*.tsbuildinfo
data/corpus/cache/
